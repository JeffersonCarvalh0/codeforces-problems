# include <iostream>
# include <string>

using namespace std;

int main() {
    int l, r;
    string input, substring;

    cin >> input;
    n = input.size();

    for (int r = 1; r < n; ++r) {
        for (int l = 0; l + r < n; ++l) {
            substring = input.substr(l, r);
            
        }
    }

}
